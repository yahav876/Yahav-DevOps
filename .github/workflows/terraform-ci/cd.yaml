name: Terraform

on: 
  push:
    branches:
    - main
  pull_request:

jobs:
  terraform:
    name: terraform 
    runs-on: ubuntu-latest 

    steps:
      - name: checkout
        uses: actions/checkout@v2
        

