apiVersion: v1
kind: Service
metadata:
  name: nginx-operated
  namespace: default
# status:
#   loadBalancer: {}
spec:
  ports:
    - name: http-web
      protocol: TCP
      port: 7788
      targetPort: http-web
  selector:
    app: testing-app
    
  type: ClusterIP

